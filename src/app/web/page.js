import { BetterBartApp } from "@/features/bart/components/BetterBartApp";

/** Renders the desktop view alias recognized by the client runtime. */
export default function WebPage() {
  return <BetterBartApp />;
}
